- Functon `with_indifferent_access` for hash with symbol or strings as keys!

```ruby
a = {age: 32}
# Try running
a[:age]
a["age"]

a = {"age": 32}
#Try running 
a[:age]
a["age"]

# Try to use with_indifferent_access! (Rails)
```